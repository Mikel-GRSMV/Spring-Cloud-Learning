server.port=7755
spring.application.name=config

#Если не локально:
#spring.cloud.config.server.git.uri=//${user.home}/application.config

spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config

eureka.client.service-url.defaultZone=${EUREKA_URI:http://localhost:7771/eureka}
eureka.client.instance.preferIpAdress=true